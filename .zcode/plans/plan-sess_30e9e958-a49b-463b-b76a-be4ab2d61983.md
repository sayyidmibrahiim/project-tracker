## Goal
Hapus menu Global Plan dari aplikasi sepenuhnya — kesalahan AI generate menu. Bukan fitur, harusnya dokumen perencanaan internal.

## Branch
`general/remove-global-plan` (cross-menu/infra per AGENTS.md branch rule).

## Removal points (9 files)

### Frontend (6)
1. **`frontend/src/lib/components/TitleBar.svelte`**
   - Hapus entry `{ id: "global-plan", label: "Global Plan" }` dari `navItems` (L37)
   - Hapus key `"global-plan"` dari `navIcons` map (L157)
   - Hapus baris shortcut help popover `Ctrl+Shift+G Global Plan` (L260)
2. **`frontend/src/App.svelte`**
   - Hapus import `GlobalPlan` (L10)
   - Hapus `"global-plan"` dari `PageId` union (L20)
   - Hapus `"global-plan"` dari `validPages` array (L92)
   - Hapus render branch `{:else if currentPage === "global-plan"} <GlobalPlan />` (L282-283)
3. **`frontend/src/lib/bridge.ts`**
   - Hapus import `GlobalPlan` (L7) — hanya jika tidak dipakai bridge lain
   - Hapus blok `globalPlanGet`/`globalPlanSave` (L290-298)
4. **`frontend/src/lib/types.ts`**
   - Hapus blok DTO `GlobalPlanStatus`/`GlobalPlanItem`/`GlobalPlan` (L187-208)
5. **`frontend/src/lib/components/WelcomeGuide.svelte`**
   - Hapus onboarding step 4 (Global Plan) (L22) — renumber step berikutnya
6. **`frontend/src/lib/components/GlobalPlan.svelte`** — DELETE file

### Backend (3)
7. **`app_web.py`**
   - Hapus import `GlobalPlanService` (L41)
   - Hapus instantiation `global_plan_svc` (L265)
   - Hapus injection `global_plan_service=global_plan_svc` (L2213)
8. **`web/js_api.py`**
   - Hapus `GlobalPlanServiceProtocol` class (L136-143)
   - Hapus constructor param `global_plan_service` (L634)
   - Hapus assignment `self._global_plan_service` (L656)
   - Hapus methods `global_plan_get` + `global_plan_save` (L2230-2246)
9. **`services/global_plan_service.py`** — DELETE file

## Notes
- Delete > edit > add (ponytail): hapus file service + komponen, jangan biarkan orphan.
- Data runtime `global_plan.json` (di app config dir) tidak di-repo → tidak dihapus, auto-ignored. Bisa dibersihkan manual oleh user kalau mau.
- Tidak menyentuh NotesEditor/extensions/markdown.ts → tidak terkena RTE Change Safety blast radius.
- Tidak ada test yang merefer Global Plan → tidak perlu hapus test.

## Verification
1. `npm --prefix frontend run build` (wajib: `web/static` gitignored, perlu rebuild setelah edit)
2. `D:/Ibrahim/Projects/project_tracker/.venv/Scripts/python.exe -m main` — jalankan app, cek:
   - Startup tanpa error
   - Titlebar: menu Global Plan hilang, menu lain (Dashboard, Project Details, Second Brain, Report, Automations, Logs, CICD, Settings) utuh
   - Klik tiap menu → navigasi normal
3. `svelte-check` (opsional, kalau ada) untuk pastikan no dangling import/type error.

## Docs sync (after user approval)
- Cek PRD.md / _docs/PROGRESS.md kalau ada mention "Global Plan" sebagai menu → update.
- Update claude-mem (decision: menu removed).