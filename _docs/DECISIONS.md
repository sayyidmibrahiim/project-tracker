# Decisions — Project Tracker

> **Purpose:** Permanent cross-session record of **why** decisions were made.
>
> - `_docs/session-notes.md` → what's active now (Now/Next/Blocked + active_menu)
> - `_docs/PROGRESS.md` → status/tracking
> - **DECISIONS.md** → locked architectural and tooling decisions with rationale

## Format

| ID       | Date         | Decision           | Rationale         | Status                            |
| -------- | ------------ | ------------------ | ----------------- | --------------------------------- |
| `D-XXXX` | `YYYY-MM-DD` | Decision statement | Why we chose this | `LOCKED` / `SUPERSEDED` / `DRAFT` |

---

## Decision Log

| ID     | Date       | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Status |
| ------ | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| D-0001 | 2026-06-28 | Navigation was implemented as a bottom dock through legacy `Sidebar.svelte`. Superseded by D-0005, which moved navigation into the custom TitleBar and removed the dock/sidebar shell.                                                                                                                                                                                                                                                                                                                      | Historical first shell direction; retained only to explain the superseded implementation. Current navigation truth is PRD §10.3 + D-0005.                                                                                                                                                                                                                                                                                                                                         | SUPERSEDED |
| D-0002 | 2026-06-28 | Memory tool = `claude-mem`. `agentmemory` is disabled to avoid duplicate capture/injection.                                                                                                                                                                                                                                                                                                                                                                                                                | Single memory layer reduces token waste and confusion. `claude-mem` is active; `agentmemory` disabled at project level.                                                                                                                                                                                                                                                                                                                                                             | LOCKED |
| D-0003 | 2026-06-28 | Single status source = `_docs/PROGRESS.md`. `PROJECT_STATUS.md` at root is a 3-line redirect. Full history in `_docs/_archive/PROJECT_STATUS_history.md`.                                                                                                                                                                                                                                                                                                                                                  | Eliminates conflicting status files. One file to read, one to update.                                                                                                                                                                                                                                                                                                                                                                                                               | LOCKED |
| D-0004 | 2026-06-29 | Config/docs cleanup: branch `chore/config-cleanup`. Cleaned stale references (notes.md, tree.txt), removed agentmemory from routing docs, tightened CLAUDE.md lifecycle rules, aligned WORKFLOW.md branch conventions.                                                                                                                                                                                                                                                                                     | Reduce token waste by removing dead references and clarifying session-vs-turn discipline. All changes are docs-only/config, 0 product code.                                                                                                                                                                                                                                                                                                                                         | LOCKED |
| D-0005 | 2026-06-29 | TitleBar replaces Sidebar. Frameless pywebview window uses a custom Svelte TitleBar; navigation and window controls live in that chrome. Native move/resize/maximize mechanics are governed by D-0015.                                                                                                                                                                                                                                                                                                           | Teams-like frameless titlebar approved by user. Bottom dock removed — nav moves to titlebar icons.                                                                                                                                                                                                                                                                                                                                                                                  | LOCKED |
| D-0006 | 2026-06-29 | All CR Number & Drone Ticket inputs use 3-icon pattern (Copy SVG + Open SVG + Edit SVG) + two-state toggle: input when empty, display label + icons when filled. Enter/blur triggers save.                                                                                                                                                                                                                                                                                                                 | Consistent UX across Dashboard, ProjectDetails, and SubProjectTable. User approved pattern from ProjectDetails CR Number identity card.                                                                                                                                                                                                                                                                                                                                             | LOCKED |
| D-0007 | 2026-06-30 | RTE editing core = Tiptap v3 (ProseMirror). NotesEditor mounts a Tiptap `Editor`; toolbar runs `editor.chain()...` commands. `frontend/src/lib/markdown.ts` (selective-sanitization renderer + `domToMarkdown` in NotesEditor) is KEPT as the Markdown↔HTML load/serialize layer for the `notes.md` contract. Custom `FontSize` mark extends the base `Mark` primitive (no official extension exists). Local image embed via the `util_choose_image` Python bridge (native file dialog → base64 data URI). | Three hand-rolled command-layer rounds each surfaced new bugs (broken undo, pending marks self-clearing, space swallowed, unreliable toggles, list markers stripped by Tailwind preflight) — systematic-debugging identified this as an architecture problem. Tiptap ships a correct history/undo-redo stack, ProseMirror marks (sub/sup/underline/strike/code), idempotent `toggleMark`/`toggleNode`, and resizable tables. All deps MIT, local npm build, fully offline (no CDN). | LOCKED |
| D-0008 | 2026-07-01 | Appcode-based folder structure with CR/Non-CR project types. Sub project renamed to Drone. State folders relocated inside CR. Non-CR projects use metadata-only state (Planning/In Progress/Done). Folder tree: `root/appcode/year/CR/STATE/project/_cr-docs + drone/UAT/PRD` + `root/appcode/year/Non-CR/project`. | User new workflow requirement. Greenfield — no migration needed. Pieces B (_cr-docs), C (approval automation), D (CICD) deferred to separate specs. | LOCKED |
| D-0009 | 2026-07-02 | Code graph integration retired from Project Tracker workflow/docs/settings. Headroom setup documented as optional local proxy (`http://localhost:8787`, `pip install "headroom-ai[proxy]"`). | User no longer wants the code graph tool. Native search/read tools remain the source lookup path; Headroom is documented for proxy setup, not as source-of-truth replacement. | LOCKED |
| D-0010 | 2026-07-03 | Project Details RTE save flow is active-file and capability-based. `Export to Word` is not product UX for active RTE files. `.md` saves Markdown, `.txt` saves UTF-8 plain text with rich formatting disabled, `.docx` is read-only until a safe Tiptap source-sync adapter exists. Legacy backend `export_to_docx` stays wired/internal until replaced. | Current DOCX path (`mammoth` import → Tiptap HTML → `htmldocx` export) is not a proven lossless editor save flow. Capability metadata prevents fake editability and stale-format saves while preserving safe direct text/Markdown writes. | LOCKED |
| D-0011 | 2026-07-04 | Red `.app-header` bar removed. Each page's white `.page-header` is the single header under the dark TitleBar. Dashboard controls (Year select, Add Year, Add Project, Refresh) live in Dashboard's `.page-header-actions`; the live clock moved to the TitleBar right cluster. Red returns to accent-only (page icon tint, active states, danger). `Header.svelte` deleted; danger/red styles unified onto `--primary-red`/`--red-hover`/`--active-red` tokens (`#DA1E28`/`#B5382F`/`#942D26` eliminated). | The red gradient wash violated DESIGN_RULES ("red is accent, not page wash", "no gradients") and duplicated per-page titles. User requested removal + design recondition 2026-07-04. PRD §10.2–10.4 rewritten to match. | LOCKED |
| D-0012 | 2026-07-04 | Per-format RTE strategy (user decision): formats Tiptap saves natively (`.md`, `.txt`) keep the direct save path; `.docx` becomes editable via the document pipeline — Tiptap JSON sidecar `<dir>/.rte/<stem>.source.json` is the source of truth, the real `.docx` is a derived background export via a custom Tiptap-JSON→python-docx mapper (`infrastructure/docx_writer.py` + `services/rte_document_service.py`). Single export worker, latest-revision-wins, hash/revision skip, atomic tmp→replace, locked-file retry. Images (all editors) stored as content-addressed asset files in `.rte/assets/` (no permanent base64); notes.md refs them as `![alt](.rte/assets/<id>.<ext>)`. Partially supersedes D-0010 (docx read-only clause); D-0007's markdown.ts layer stays for `.md`. Legacy `get_rte_file`/`save_rte_file`/`export_to_docx` remain wired. | flow-tiptap.md architecture: export failure must never lose content (source saved first, DOCX derived); Tiptap Pro rejected — office laptop blocks GitHub/PyPI/paid registries, so exporter must be free/offline (python-docx already bundled). | LOCKED |
| D-0013 | 2026-07-06 | Piece C approval automation: per-project automation_enabled + approval_templates in project_data.json; polling jobs persisted in SQLite approval_polling_jobs (resume on startup); reply matched by CR number in subject + received-after-sent; .msg saved via Outlook SaveAs olMSG into _cr-docs/ | Spec 2026-07-02 approved; SQLite chosen by master plan for job state (job state is operational, cache stays rebuildable — a lost job row only stops an in-flight poll) | LOCKED |
| D-0014 | 2026-07-09 | Piece D CICD Bitbucket integration (`services/cicd_service.py`, `CICDBrowser.svelte`, 9th nav): git work via **stdlib `subprocess` (no new dependency)** — office network blocks GitHub/PyPI, python-git libs disallowed; **first subprocess in the app sets the `CREATE_NO_WINDOW` precedent** (no console flash on Windows). Clone runs on a **daemon thread** with a poll-able job dict (`start_clone` returns immediately, frontend polls `cicd_clone_status` every 1.2s) rather than blocking the bridge — a network clone over VPN must not freeze the webview; pattern mirrors the RTE export poll. `_CicdServiceAdapter` **reuses `get_appcode_config`** to resolve each appcode's CICD dir (`per_appcode`→`{appcode}/CICD`, `shared_root`→`AppCodeConfig.cicd_shared_path`). Spec deviations, all deliberate: (1) CICD-location switch UI on the CICD page via `appcode_update_config`, not Settings — the config is per-appcode, Settings is global; (2) file open **reuses the existing `file_open` bridge**, no new `open_repo_file`; (3) ONE consolidated `tests/test_cicd_service.py`, not the spec's three files; (4) cloned folder names strip `.git` and clone adds `--single-branch` (lighter fetch); (5) directory tree nodes carry no aggregate git badge (files only). DEFAULT AMAN: git absent → install-steps empty state + Recheck; non-git/porcelain failure → files without status; clone into existing folder → rejected before spawn. Create Drone Ticket (Jenkins) stays out of scope. | Spec `_docs/specs/superpowers/specs/2026-07-02-cicd-bitbucket-design.md`; stdlib-only + offline constraint (`office-laptop-offline-constraint`); background clone prevents UI freeze; reuse over new (smallest diff). | LOCKED |
| D-0015 | 2026-07-10 | Frameless Windows shell follows native desktop behavior. Empty custom-titlebar chrome starts the OS caption loop with `WM_NCLBUTTONDOWN/HTCAPTION`; edge/corner zones use native HT resize codes; WinForms `Form.WindowState` is the maximize/restore source of truth. A guarded `GWLP_WNDPROC` hook removes the non-client frame and handles `WM_GETMINMAXINFO` from the active monitor's `rcWork`, so maximized windows respect any non-autohide taskbar. Resize hit targets are absent/rejected while maximized; minimum size remains 960×640. Interactive titlebar descendants never start drag or double-click maximize. | Windows owns movement, Aero Snap, resize cursors, min-size enforcement, and restore geometry more reliably than JavaScript deltas or WebView2 CSS drag regions. `-webkit-app-region: drag` was ineffective under pywebview 6.2.1/WebView2, while asynchronous post-mousemove forwarding arrived too late. User live-verified drag/Snap, repeated maximize/restore, taskbar work area, guards, native resizing, and minimum size on 2026-07-10. | LOCKED |
